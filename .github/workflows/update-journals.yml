name: Update Journal Data

on:
  schedule:
    - cron: '0 6 1 * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - run: pip install -r scripts/requirements.txt

      - run: python scripts/build_journals.py

      - name: Copy to extension bundle
        run: cp data/journals.json extension/journals.json

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/journals.json extension/journals.json
          git diff --cached --quiet || git commit -m "chore: update journal data $(date +%Y-%m-%d)"
          git push
